name: Check User

on:
  push:
    branches: [ "lp/apredendendo-CI/CD" ]

jobs:
  check_user:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Check user
      run: |
        whoami
